#!/usr/bin/env python3
"""A module containing a single function to
calculate the floor of a given number."""
import math


def floor(n: float) -> int:
    """Returns the largest integer not greater than the input number."""
    return math.floor(n)
